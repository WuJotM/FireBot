module.exports = {
    name: "show",
    once: false,
    async execute(client, interaction, kazagumo) {
        try {
            
        } catch (err) {
            return console.error(err);
        }
    }
}